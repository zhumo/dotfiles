Attempt to cleanup the following worktree: $ARGUMENTS

The worktree should be merged into master. Then the worktree should be discarded and its separate repo deleted.

If there are still uncommitted changes then warn the user and do not proceed.

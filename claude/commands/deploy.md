Your goal is to deploy this app. Search around for deployment.
1. Look in the README.md or CLAUDE.md file for deploy instructions
2. Any files that are called `bin/deploy.sh` or maybe `npm run deploy` or other such built-in deploy commands

If no such information exists, ask the user for details on how to deploy.

$ARGUMENTS

Create a new worktree for this feature. Put the worktree in a folder that a sibling of the current repo. After creating the new worktree, cd into the new worktree as your new working directory.
